version: 1
mode: flat
root: /bryck/cloudcp_fallback/zero_byte
threads: 4
seed: 42

# ZERO tier: exactly 0-byte files. Tests empty-object / zero-length PUT.
content:
  type: sparse          # nothing written; ftruncate to 0

naming:
  charset: ascii
  alphabet: [lower, digit]
  prefix: "zero-"
  length: 10

flat:
  num_files: 200

size:
  type: fixed
  bytes: 0

extensions:
  - ext: .bin
    weight: 1
